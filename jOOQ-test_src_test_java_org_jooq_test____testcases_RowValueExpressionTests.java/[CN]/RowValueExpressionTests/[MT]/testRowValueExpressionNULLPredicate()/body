{
  assertEquals(3,(int)create().selectCount().from(TBook()).where(row(TBook_CONTENT_PDF(),TBook_CONTENT_TEXT()).isNull()).and(row(TBook_ID(),TBook_AUTHOR_ID()).isNotNull()).fetchOne(0,Integer.class));
}
