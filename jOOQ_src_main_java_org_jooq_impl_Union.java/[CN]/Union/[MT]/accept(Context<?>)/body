{
  for (int i=0; i < queries.size(); i++) {
    if (i != 0) {
      ctx.formatSeparator().keyword(operator.toSQL(ctx.configuration().dialect())).formatSeparator();
    }
    wrappingParenthesis(ctx,"(");
    ctx.visit(queries.get(i));
    wrappingParenthesis(ctx,")");
  }
}
