{
  for (int i=0; i < queries.size(); i++) {
    if (i != 0) {
      context.formatSeparator().keyword(operator.toSQL(context.getDialect())).formatSeparator();
    }
    wrappingParenthesis(context,"(");
    context.sql(queries.get(i));
    wrappingParenthesis(context,")");
  }
}
