{
  StringBuilder sb=new StringBuilder();
  for (  byte b : binary) {
    sb.append("\\\\");
    int octal=b & 0x000000ff;
    sb.append(leftPad(toOctalString(octal),3,'0'));
  }
  return sb.toString();
}
