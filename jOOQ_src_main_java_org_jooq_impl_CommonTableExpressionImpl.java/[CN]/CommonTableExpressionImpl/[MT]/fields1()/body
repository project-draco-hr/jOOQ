{
  List<Field<?>> s=select.getSelect();
  Field<?>[] f=new Field[s.size()];
  for (int i=0; i < f.length; i++)   f[i]=fieldByName(s.get(i).getDataType(),name.name,name.fieldNames[i]);
  Fields<R> result=new Fields<R>(f);
  return result;
}
