{
  List<UDTDefinition> result=new ArrayList<UDTDefinition>();
  for (  String name : create().selectDistinct(UDT_NAME).from(ATTRIBUTES).where(UDT_SCHEMA.equal(getSchemaName())).orderBy(UDT_NAME).fetch(UDT_NAME)) {
    result.add(new PostgresUDTDefinition(this,name,null));
  }
  return result;
}
