{
  for (  Table<?> table : getFrom()) {
    if (!knownTable(table)) {
      return false;
    }
  }
  for (  Join j : getJoin()) {
    if (!knownTable(j.getTable())) {
      return false;
    }
  }
  return true;
}
