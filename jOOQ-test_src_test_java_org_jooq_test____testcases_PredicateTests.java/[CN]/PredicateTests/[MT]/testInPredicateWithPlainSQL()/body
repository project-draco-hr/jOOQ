{
  Result<Record1<Integer>> result=create().select(TBook_ID()).from(TBook()).where(field(TBook_ID().getName()).in(1,2)).or(field(TBook_TITLE().getName()).in(null,null)).orderBy(TBook_ID()).fetch();
  assertEquals(2,result.size());
  assertEquals(asList(1,2),result.getValues(TBook_ID()));
}
