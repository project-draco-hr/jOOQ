{
  if (length == l) {
    return this;
  }
 else   if (hasLength()) {
    return new DefaultDataType<T>(dialect,sqlDataType,type,typeName,castTypeName,precision,scale,l);
  }
 else {
    return this;
  }
}
