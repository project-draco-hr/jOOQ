{
  StringBuilder sb=new StringBuilder();
  String s1="";
  String s2="";
  if (path.from != null && !"DSL".equals(path.from)) {
    s1=path.from;
  }
  s2=expr.toString();
  sb.append(s1);
  if (!s1.isEmpty() && !s2.isEmpty())   sb.append(" ");
  sb.append(s2);
  return sb.toString();
}
