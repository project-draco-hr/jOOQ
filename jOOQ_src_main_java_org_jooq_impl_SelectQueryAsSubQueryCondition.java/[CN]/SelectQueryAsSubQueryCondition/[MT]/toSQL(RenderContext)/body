{
  if (context.subquery()) {
    context.sql(field).sql(" ").keyword(operator.toSQL()).sql(" (").sql(query).sql(")");
  }
 else {
    context.sql(field).sql(" ").keyword(operator.toSQL()).sql(" (").subquery(true).sql(query).subquery(false).sql(")");
  }
}
