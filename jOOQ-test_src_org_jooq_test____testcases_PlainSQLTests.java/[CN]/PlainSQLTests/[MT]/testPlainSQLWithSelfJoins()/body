{
  Result<Record> result=create().fetch("select * from (select id from t_author) a1, (select id from t_author) a2 order by a1.id, a2.id");
  assertEquals(asList(1,1,2,2),result.getValues(0,int.class));
  assertEquals(asList(1,2,1,2),result.getValues(1,int.class));
}
