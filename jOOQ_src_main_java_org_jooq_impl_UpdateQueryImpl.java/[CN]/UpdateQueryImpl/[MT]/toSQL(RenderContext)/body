{
  context.keyword("update ").declareTables(true).sql(getInto()).declareTables(false).keyword(" set ").sql(updateMap);
  if (!(getWhere() instanceof TrueCondition)) {
    context.keyword(" where ").sql(getWhere());
  }
}
