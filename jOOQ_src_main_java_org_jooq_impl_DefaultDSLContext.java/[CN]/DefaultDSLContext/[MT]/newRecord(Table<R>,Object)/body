{
  return Utils.newRecord(table,configuration).initialise(new RecordInitialiser<R,RuntimeException>(){
    @Override public R initialise(    R record){
      record.from(source);
      return record;
    }
  }
);
}
