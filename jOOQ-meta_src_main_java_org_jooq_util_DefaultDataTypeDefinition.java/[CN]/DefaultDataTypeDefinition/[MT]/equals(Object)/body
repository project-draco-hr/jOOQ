{
  if (this == obj)   return true;
  if (obj instanceof DefaultDataTypeDefinition) {
    DefaultDataTypeDefinition other=(DefaultDataTypeDefinition)obj;
    if (normalise(type).equals(normalise(other.type)) && normalise(userType).equals(normalise(other.userType))) {
      return true;
    }
  }
  return false;
}
