{
  jOOQAbstractTest.reset=false;
  assertEquals(4,create(new FailOnMissingWhere()).update(TBook()).set(TBook_TITLE(),"changed").where("1 = 1").execute());
  try {
    create(new FailOnMissingWhere()).update(TBook()).set(TBook_TITLE(),"changed").execute();
    fail();
  }
 catch (  MissingWhereException expected) {
  }
}
