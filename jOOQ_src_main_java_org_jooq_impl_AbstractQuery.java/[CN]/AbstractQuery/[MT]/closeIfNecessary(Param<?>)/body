{
  if (keepStatement() && statement != null) {
    if (param.isInline()) {
      close();
    }
 else     if (SettingsTools.executeStaticStatements(configuration().settings())) {
      close();
    }
  }
}
