{
  context.keyword("insert ").keyword((onDuplicateKeyIgnore && asList(MARIADB,MYSQL).contains(context.configuration().dialect())) ? "ignore " : "").keyword("into ").sql(getInto()).sql(" ").sql(insertMaps);
  toSQLReturning(context);
}
