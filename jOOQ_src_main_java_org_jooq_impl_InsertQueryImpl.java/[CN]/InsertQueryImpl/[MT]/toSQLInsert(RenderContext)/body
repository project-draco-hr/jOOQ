{
  context.keyword("insert ").keyword((onDuplicateKeyIgnore && asList(MARIADB,MYSQL).contains(context.configuration().dialect())) ? "ignore " : "").keyword("into ").visit(getInto()).sql(" ").visit(insertMaps);
  toSQLReturning(context);
}
