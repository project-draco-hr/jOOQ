{
  context.keyword("insert ").keyword((onDuplicateKeyIgnore && context.configuration().getDialect() == MYSQL) ? "ignore " : "").keyword("into ").sql(getInto()).sql(" ").sql(insertMaps);
  toSQLReturning(context);
}
