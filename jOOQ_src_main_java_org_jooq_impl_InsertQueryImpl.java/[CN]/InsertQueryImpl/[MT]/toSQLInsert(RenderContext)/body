{
  context.keyword("insert ").keyword((onDuplicateKeyIgnore && context.configuration().dialect() == MYSQL) ? "ignore " : "").keyword("into ").sql(getInto()).sql(" ").sql(insertMaps);
  toSQLReturning(context);
}
