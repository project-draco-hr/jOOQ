{
  if (onDuplicateKeyUpdate) {
switch (context.configuration().dialect().family()) {
case CUBRID:
case MARIADB:
case MYSQL:
{
        toSQLInsert(context);
        context.formatSeparator().keyword("on duplicate key update ").visit(updateMap);
        break;
      }
case H2:
{
      throw new SQLDialectNotSupportedException("The ON DUPLICATE KEY UPDATE clause cannot be simulated for " + context.configuration().dialect());
    }
case DB2:
case HSQLDB:
case ORACLE:
case SQLSERVER:
case SYBASE:
{
    context.visit(toMerge(context.configuration()));
    break;
  }
default :
throw new SQLDialectNotSupportedException("The ON DUPLICATE KEY UPDATE clause cannot be simulated for " + context.configuration().dialect());
}
}
 else if (onDuplicateKeyIgnore) {
switch (context.configuration().dialect().family()) {
case MARIADB:
case MYSQL:
{
toSQLInsert(context);
break;
}
case CUBRID:
{
FieldMapForUpdate update=new FieldMapForUpdate();
Field<?> field=getInto().field(0);
update.put(field,field);
toSQLInsert(context);
context.formatSeparator().keyword("on duplicate key update ").visit(update);
break;
}
case H2:
{
throw new SQLDialectNotSupportedException("The ON DUPLICATE KEY IGNORE clause cannot be simulated for " + context.configuration().dialect());
}
case DB2:
case HSQLDB:
case ORACLE:
case SQLSERVER:
case SYBASE:
{
context.visit(toMerge(context.configuration()));
break;
}
default :
throw new SQLDialectNotSupportedException("The ON DUPLICATE KEY IGNORE clause cannot be simulated for " + context.configuration().dialect());
}
}
 else {
toSQLInsert(context);
}
}
