{
switch (configuration.dialect()) {
case CUBRID:
case DB2:
case FIREBIRD:
case ORACLE:
case SQLSERVER:
case SYBASE:
    return DSL.decode().when(condition,inline(true)).otherwise(inline(false));
case DERBY:
case H2:
case HSQLDB:
case MYSQL:
case POSTGRES:
case SQLITE:
case ASE:
case INGRES:
  return condition;
}
return condition;
}
