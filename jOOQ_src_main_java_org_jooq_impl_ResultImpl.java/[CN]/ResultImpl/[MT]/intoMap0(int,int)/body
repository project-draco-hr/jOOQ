{
  Map<K,V> map=new LinkedHashMap<K,V>();
  for (  R record : this)   if (map.put((K)record.getValue(kIndex),(V)record.getValue(vIndex)) != null)   throw new InvalidResultException("Key " + kIndex + " is not unique in Result for "+ this);
  return map;
}
