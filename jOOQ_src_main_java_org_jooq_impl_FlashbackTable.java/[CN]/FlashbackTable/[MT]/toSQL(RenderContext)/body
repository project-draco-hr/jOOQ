{
  context.sql(table);
  if (asOf != null) {
    context.sql(" ").keyword("as of").sql(" ").sql(type).sql(" ").sql(asOf);
  }
 else {
    context.sql(" ").keyword("versions between").sql(" ").sql(type).sql(" ").sql(minvalue).sql(" ").keyword("and").sql(" ").sql(maxvalue);
  }
}
