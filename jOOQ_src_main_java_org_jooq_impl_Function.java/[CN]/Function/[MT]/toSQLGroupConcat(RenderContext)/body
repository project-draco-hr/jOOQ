{
  toSQLFunctionName(context);
  context.sql("(");
  if (distinct) {
    context.keyword("distinct ");
  }
  context.visit(arguments.get(0));
  if (!withinGroupOrderBy.isEmpty()) {
    context.keyword(" order by ").visit(withinGroupOrderBy);
  }
  if (arguments.size() > 1) {
    context.keyword(" separator ").visit(arguments.get(1));
  }
  context.sql(")");
}
