{
  context.keyword("insert into").sql(" ").visit(into).sql(" (");
  String separator="";
  for (  Field<?> field : fields) {
    context.sql(separator).literal(field.getName());
    separator=", ";
  }
  context.sql(")").formatSeparator().visit(select);
}
