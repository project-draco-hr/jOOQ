{
  if (!returning.isEmpty()) {
switch (context.configuration().dialect()) {
case FIREBIRD:
case POSTGRES:
      context.formatSeparator().keyword("returning ").visit(returning);
    break;
default :
}
}
}
