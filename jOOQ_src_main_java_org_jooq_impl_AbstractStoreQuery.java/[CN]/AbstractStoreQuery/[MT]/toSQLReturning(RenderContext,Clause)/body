{
  if (!returning.isEmpty()) {
switch (context.configuration().dialect()) {
case FIREBIRD:
case POSTGRES:
      context.formatSeparator().start(clause).keyword("returning").sql(" ").visit(returning).end(clause);
    break;
default :
}
}
}
