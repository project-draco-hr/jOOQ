{
  boolean inline=context.inline();
  boolean declareFields=context.declareFields();
  boolean declareTables=context.declareFields();
  context.declareTables(true).sql(table).declareTables(declareTables).keyword(" pivot(").inline(true).declareFields(true).sql(aggregateFunctions).keyword(" for ").literal(on.getName()).keyword(" in (").sql(in).declareFields(declareFields).inline(inline).sql("))");
}
