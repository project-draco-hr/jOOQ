{
  ParamType paramType=context.paramType();
  boolean declareFields=context.declareFields();
  boolean declareTables=context.declareTables();
  context.declareTables(true).sql(table).declareTables(declareTables).formatSeparator().keyword("pivot (").paramType(INLINED).declareFields(true).formatIndentStart().sql(aggregateFunctions).formatSeparator().keyword("for ").literal(on.getName()).formatSeparator().keyword("in (").sql(in).declareFields(declareFields).paramType(paramType).sql(")").formatIndentEnd().formatNewLine().sql(")");
}
