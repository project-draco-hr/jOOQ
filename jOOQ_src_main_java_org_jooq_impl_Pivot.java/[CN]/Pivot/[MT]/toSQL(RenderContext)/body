{
  boolean inline=context.inline();
  boolean declare=context.declareFields();
  context.sql(table).sql(" pivot(").inline(true).declareFields(true).sql(aggregateFunctions).sql(" for ").literal(over.getName()).sql(" in (").sql(in).declareFields(declare).inline(inline).sql("))");
}
