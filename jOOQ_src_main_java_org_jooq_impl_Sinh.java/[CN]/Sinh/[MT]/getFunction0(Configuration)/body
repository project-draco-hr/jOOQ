{
switch (configuration.getDialect()) {
case ADAPTIVESERVER:
case HSQLDB:
case INGRES:
case MYSQL:
case POSTGRES:
case SQLSERVER:
case SYBASE:
{
      Field<Integer> one=create(configuration).one();
      Field<Integer> two=create(configuration).two();
      return argument.mul(two).exp().sub(one).div(argument.exp().mul(two));
    }
default :
  return new Function<BigDecimal>("sinh",SQLDataType.NUMERIC,argument);
}
}
