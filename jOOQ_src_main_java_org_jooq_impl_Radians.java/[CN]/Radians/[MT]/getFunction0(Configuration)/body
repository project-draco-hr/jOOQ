{
switch (configuration.getDialect()) {
case INGRES:
case ORACLE:
    return argument.cast(BigDecimal.class).mul(create(configuration).pi()).div(180);
default :
  return new Function<BigDecimal>("radians",SQLDataType.NUMERIC,argument);
}
}
