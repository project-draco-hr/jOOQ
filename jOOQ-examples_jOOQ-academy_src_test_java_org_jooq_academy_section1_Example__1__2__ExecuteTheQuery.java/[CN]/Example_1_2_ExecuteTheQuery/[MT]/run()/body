{
  Tools.title("Selecting FIRST_NAME and LAST_NAME from the AUTHOR table");
  Tools.print(DSL.using(connection()).select(AUTHOR.FIRST_NAME,AUTHOR.LAST_NAME).from(AUTHOR).orderBy(AUTHOR.ID).fetch());
}
