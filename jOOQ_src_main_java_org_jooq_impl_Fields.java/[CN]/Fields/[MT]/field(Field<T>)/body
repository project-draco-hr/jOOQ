{
  if (field == null) {
    return null;
  }
  for (  Field<?> f : fields) {
    if (f.equals(field)) {
      return (Field<T>)f;
    }
  }
  String name=field.getName();
  for (  Field<?> f1 : fields) {
    if (f1.getName().equals(name)) {
      return (Field<T>)f1;
    }
  }
  return null;
}
