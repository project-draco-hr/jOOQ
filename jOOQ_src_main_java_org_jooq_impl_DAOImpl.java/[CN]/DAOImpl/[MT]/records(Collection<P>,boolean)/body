{
  List<R> result=new ArrayList<R>();
  Field<?>[] pk=pk();
  for (  P object : objects) {
    R record=using(configuration).newRecord(table,object);
    if (forUpdate && pk != null)     for (    Field<?> field : pk)     record.changed(field,false);
    for (int i=0; i < record.size(); i++)     if (record.getValue(i) == null)     if (!record.field(i).getDataType().nullable())     record.changed(i,false);
    result.add(record);
  }
  return result;
}
