{
  if (query != null) {
    return query;
  }
 else {
switch (context.getDialect()) {
case POSTGRES:
{
        return array;
      }
case H2:
case HSQLDB:
default :
{
      return create(context).select().from(table(array));
    }
}
}
}
