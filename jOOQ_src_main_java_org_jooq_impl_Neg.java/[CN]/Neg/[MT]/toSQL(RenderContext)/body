{
  SQLDialect dialect=context.getDialect();
  if (operator == BIT_NOT && asList(H2,HSQLDB,INGRES,ORACLE).contains(dialect)) {
    context.sql("(0 -").sql(field).sql(" - 1)");
  }
 else   if (operator == BIT_NOT && dialect == DB2) {
    context.keyword("bitnot(").sql(field).sql(")");
  }
 else   if (operator == BIT_NOT && dialect == FIREBIRD) {
    context.keyword("bin_not(").sql(field).sql(")");
  }
 else {
    context.sql(operator.toSQL()).sql("(").sql(field).sql(")");
  }
}
