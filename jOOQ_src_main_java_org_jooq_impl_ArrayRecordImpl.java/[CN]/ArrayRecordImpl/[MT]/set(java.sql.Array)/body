{
  if (array == null) {
    this.array=null;
  }
 else {
    Object o;
    if (schema != null) {
      o=array.getArray(schema.getTypeMapping());
    }
 else {
      o=array.getArray();
    }
    this.array=Convert.convert(o,type.getArrayType());
  }
}
