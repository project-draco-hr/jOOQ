{
  if (ctx.qualify()) {
    String separator="";
    for (    String string : sql) {
      ctx.sql(separator);
      ctx.literal(string);
      separator=".";
    }
  }
 else {
    ctx.literal(sql[sql.length - 1]);
  }
}
