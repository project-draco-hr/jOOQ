{
  @SuppressWarnings("deprecation") SQLDialect dialect=SQLDialect.SQL99;
  try {
    dialect=SQLDialect.valueOf(getProperties().getProperty(DIALECT_PARAM));
  }
 catch (  Exception ignore) {
  }
  return DSL.using(dialect);
}
