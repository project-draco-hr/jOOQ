{
  context.visit(row).sql(not ? " " : "").keyword(not ? "not" : "").sql(" ").keyword("between").sql(" ").keyword(symmetric ? "symmetric" : "").sql(symmetric ? " " : "").visit(minValue).sql(" ").keyword("and").sql(" ").visit(maxValue);
}
