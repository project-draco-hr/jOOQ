{
  if (context.getDialect() == SQLDialect.DERBY) {
    if (field.getDataType().isNumeric() && VARCHAR.equals(getSQLDataType())) {
      context.keyword("trim(cast(").keyword("cast(").sql(field).keyword(" as char(38))").keyword(" as ").keyword(getDataType(context).getCastTypeName(context)).sql("))");
      return;
    }
 else     if (field.getDataType().isString() && asList(FLOAT,DOUBLE,REAL).contains(getSQLDataType())) {
      context.keyword("cast(").keyword("cast(").sql(field).keyword(" as decimal)").keyword(" as ").keyword(getDataType(context).getCastTypeName(context)).sql(")");
      return;
    }
 else     if (field.getDataType().isNumeric() && BOOLEAN.equals(getSQLDataType())) {
      context.sql(asDecodeNumberToBoolean());
      return;
    }
 else     if (field.getDataType().isString() && BOOLEAN.equals(getSQLDataType())) {
      context.sql(asDecodeVarcharToBoolean());
      return;
    }
  }
  context.keyword("cast(").sql(field).keyword(" as ").keyword(getDataType(context).getCastTypeName(context)).sql(")");
}
