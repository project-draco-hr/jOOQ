{
  if (context.getDialect() == SQLDialect.DERBY) {
    if (field.getDataType().isNumeric() && BOOLEAN.equals(getSQLDataType())) {
      context.bind(asDecodeNumberToBoolean(context));
      return;
    }
 else     if (field.getDataType().isString() && BOOLEAN.equals(getSQLDataType())) {
      context.bind(asDecodeVarcharToBoolean(context));
      return;
    }
  }
  context.bind(field);
}
