{
  if (!dataFetched) {
    dataFetched=true;
    try {
      data=create().select().from(delegate.getTable()).orderBy(field(getPrimaryKeyColumn().getName())).fetch();
    }
 catch (    DataAccessException e) {
      log.error("Error while initialising master data",e);
    }
  }
  return data;
}
