{
  settings=(settings != null) ? settings : new Settings();
  settings.withExecution(new Execution().withStatementType(StatementType.STATIC_STATEMENT));
  return new TestFactory(getConnection(),settings);
}
