{
  reset=false;
  assertEquals(3,create().insertInto(T_959.T_959).set(T_959.JAVA_KEYWORDS,T_959JavaKeywords.public_).newRecord().set(T_959.JAVA_KEYWORDS,T_959JavaKeywords.abstract_).newRecord().set(T_959.JAVA_KEYWORDS,T_959JavaKeywords.class_).execute());
  List<T_959JavaKeywords> result=create().selectFrom(T_959.T_959).orderBy(T_959.JAVA_KEYWORDS).fetch(T_959.JAVA_KEYWORDS);
  assertEquals(3,result.size());
  assertEquals(T_959JavaKeywords.abstract_,result.get(0));
  assertEquals(T_959JavaKeywords.class_,result.get(1));
  assertEquals(T_959JavaKeywords.public_,result.get(2));
}
