{
  String op=operator.toSQL();
  if (operator == BIT_XOR && context.configuration().getDialect() == POSTGRES) {
    op="#";
  }
  context.sql("(");
  context.sql(lhs);
  for (  Field<?> field : rhs) {
    context.sql(" ").sql(op).sql(" ").sql(field);
  }
  context.sql(")");
}
