{
  settings=(settings != null) ? settings : new Settings();
  settings.withRenderMapping(new RenderMapping().withSchemata(new MappedSchema().withInput(TAuthor().getSchema().getName()).withOutput(Public.PUBLIC.getName())));
  return new PublicFactory(getConnection(),settings);
}
