{
  if (field == null) {
    return null;
  }
  for (  Field<?> f : this) {
    if (f.equals(field)) {
      return (Field<T>)f;
    }
  }
  String name=field.getName();
  for (  Field<?> f1 : this) {
    if (f1.getName().equals(name)) {
      return (Field<T>)f1;
    }
  }
  return null;
}
