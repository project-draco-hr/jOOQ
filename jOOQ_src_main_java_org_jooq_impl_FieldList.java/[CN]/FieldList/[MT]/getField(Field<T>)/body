{
  if (field == null) {
    return null;
  }
  Field<?> result=null;
  String name=field.getName();
  for (  Field<?> f1 : this) {
    if (f1.getName().equals(name)) {
      if (result == null) {
        result=f1;
      }
 else {
        for (        Field<?> f2 : this) {
          if (f2.equals(field)) {
            result=f2;
            break;
          }
        }
        break;
      }
    }
  }
  return (Field<T>)result;
}
