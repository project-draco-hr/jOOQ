{
  if (h2Style) {
    if (ctx.configuration().dialect() == H2) {
      toSQLH2(ctx);
    }
 else {
      ctx.visit(getStandardMerge(ctx.configuration()));
    }
  }
 else {
    toSQLStandard(ctx);
  }
}
