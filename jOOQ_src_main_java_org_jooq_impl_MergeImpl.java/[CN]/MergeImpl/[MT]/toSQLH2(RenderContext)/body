{
  context.keyword("merge into ").declareTables(true).visit(table).formatSeparator();
  context.sql("(");
  Utils.fieldNames(context,getH2Fields());
  context.sql(")");
  if (!getH2Keys().isEmpty()) {
    context.keyword(" key (");
    Utils.fieldNames(context,getH2Keys());
    context.sql(")");
  }
  if (h2Select != null) {
    context.sql(" ").visit(h2Select);
  }
 else {
    context.keyword(" values (").visit(getH2Values()).sql(")");
  }
}
