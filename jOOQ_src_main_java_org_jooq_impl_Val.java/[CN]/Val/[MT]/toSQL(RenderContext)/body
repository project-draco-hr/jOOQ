{
switch (context.castMode()) {
case NEVER:
    toSQL(context,value,getType());
  return;
case ALWAYS:
toSQLCast(context);
return;
case SOME:
if (context.cast()) {
toSQLCast(context);
}
 else if (shouldCast(context)) {
toSQLCast(context);
}
 else {
toSQL(context,value,getType());
}
return;
}
if (shouldCast(context)) {
toSQLCast(context);
}
 else {
toSQL(context,value,getType());
}
}
