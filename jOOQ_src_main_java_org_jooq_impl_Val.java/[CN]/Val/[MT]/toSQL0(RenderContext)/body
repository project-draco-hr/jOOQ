{
switch (context.castMode()) {
case NEVER:
    toSQL(context,value,getConverter());
  return;
case ALWAYS:
toSQLCast(context);
return;
}
if (shouldCast(context)) {
toSQLCast(context);
}
 else {
toSQL(context,value,getConverter());
}
}
