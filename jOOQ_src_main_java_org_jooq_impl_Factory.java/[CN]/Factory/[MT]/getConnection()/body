{
  if (connection == null) {
    return null;
  }
 else   if (connection.getClass() == ConnectionProxy.class) {
    return connection;
  }
 else {
    return new ConnectionProxy(connection,settings);
  }
}
