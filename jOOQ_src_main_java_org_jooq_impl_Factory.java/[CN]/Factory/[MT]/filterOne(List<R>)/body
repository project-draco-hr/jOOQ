{
  if (list.size() == 0) {
    return null;
  }
 else   if (list.size() == 1) {
    return list.get(0);
  }
 else {
    throw new DataAccessException("Too many rows returned : " + list.size());
  }
}
