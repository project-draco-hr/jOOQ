{
switch (dialect().family()) {
case ASE:
case DB2:
case INGRES:
case ORACLE:
case SQLSERVER:
case SYBASE:
case CUBRID:
case DERBY:
case FIREBIRD:
case H2:
case HSQLDB:
case MARIADB:
case MYSQL:
case SQLITE:
    log.info("SKIPPING","TRUNCATE CASCADE tests");
  return;
}
jOOQAbstractTest.reset=false;
try {
create().truncate(TAuthor()).restrict().execute();
}
 catch (Exception expected) {
}
create().truncate(TAuthor()).cascade().execute();
assertEquals(0,create().fetch(TAuthor()).size());
assertEquals(0,create().fetch(TBook()).size());
}
