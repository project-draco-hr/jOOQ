{
  QueryOrigin origin=defaultIfNull(RECURSION_LOCK.get(),APPLICATION);
  if (log == null) {
    log=new QueryLog(ctx.query(),origin,endPrepareTime - startPrepareTime,endBindTime - startBindTime,endExecuteTime - startExecuteTime);
  }
  return log;
}
