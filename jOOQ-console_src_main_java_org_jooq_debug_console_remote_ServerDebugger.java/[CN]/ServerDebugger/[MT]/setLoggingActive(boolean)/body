{
  if (isActive) {
    setLoggingListener(new LoggingListener(){
      @Override public void logQueries(      StatementLog statementLog){
        comm.asyncExec(new CMC_logQueries(),statementLog);
      }
      @Override public void logResultSet(      int sqlQueryDebuggerDataID,      ResultSetLog resultSetLog){
        comm.asyncExec(new CMC_logResultSet(),sqlQueryDebuggerDataID,resultSetLog);
      }
    }
);
  }
 else {
    setLoggingListener(null);
  }
}
