{
  if (qualifiedName.length != 1)   throw new IllegalStateException("Cannot create a CorrelationName from a qualified name : " + Arrays.asList(qualifiedName));
  return new CorrelationNameImpl(qualifiedName[0],fieldNames);
}
