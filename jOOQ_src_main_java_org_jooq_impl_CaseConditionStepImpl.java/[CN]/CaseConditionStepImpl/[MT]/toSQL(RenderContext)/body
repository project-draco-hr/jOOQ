{
  context.sql("case");
  for (int i=0; i < conditions.size(); i++) {
    context.sql(" when ").sql(conditions.get(i)).sql(" then ").sql(results.get(i));
  }
  if (otherwise != null) {
    context.sql(" else ").sql(otherwise);
  }
  context.sql(" end");
}
