{
  context.keyword("case");
  for (int i=0; i < conditions.size(); i++) {
    context.keyword(" when ").sql(conditions.get(i)).keyword(" then ").sql(results.get(i));
  }
  if (otherwise != null) {
    context.keyword(" else ").sql(otherwise);
  }
  context.keyword(" end");
}
