{
switch (configuration.getDialect()) {
case ASE:
case SQLSERVER:
    return new Function<BigDecimal>("atn2",SQLDataType.NUMERIC,arg1,arg2);
default :
  return new Function<BigDecimal>("atan2",SQLDataType.NUMERIC,arg1,arg2);
}
}
