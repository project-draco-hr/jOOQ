{
  if (StringUtils.isBlank(getSchema().getName())) {
    return getContainer().getName() + "." + getName();
  }
 else {
    return getDatabase().getOutputSchema(getSchema().getName()) + "." + getContainer().getName()+ "."+ getName();
  }
}
