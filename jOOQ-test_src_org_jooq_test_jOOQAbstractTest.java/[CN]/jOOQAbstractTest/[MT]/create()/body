{
  Settings settings=SettingsTools.defaultSettings().withRenderMapping(new RenderMapping().withDefaultSchema("")).withExecuteListeners(TestStatisticsListener.class.getName(),DebugListener.class.getName());
  return create(settings);
}
