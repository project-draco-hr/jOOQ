{
  checkNotClosed();
  MockExecuteContext context=new MockExecuteContext(new String[]{localSql},new Object[][]{bindings().toArray()},localAutoGeneratedKeys,localColumnIndexes,localColumnNames,unbox(outParameterTypes));
  result=data.execute(context);
  return result != null && result.length > 0 && result[resultIndex].data != null;
}
