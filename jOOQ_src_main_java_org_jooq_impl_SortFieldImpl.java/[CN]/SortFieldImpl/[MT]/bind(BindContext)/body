{
  if (nullsFirst || nullsLast) {
switch (context.configuration().dialect().family()) {
case DB2:
case ASE:
case CUBRID:
case INGRES:
case MARIADB:
case MYSQL:
case SQLITE:
case SQLSERVER:
case SYBASE:
{
        context.visit(field);
      }
  }
}
context.visit(field);
}
