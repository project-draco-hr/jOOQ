{
  if (nullsFirst || nullsLast) {
switch (context.configuration().dialect().family()) {
case ASE:
case DB2:
case INGRES:
case SQLSERVER:
case SYBASE:
case CUBRID:
case MARIADB:
case MYSQL:
case SQLITE:
{
        context.visit(field);
      }
  }
}
context.visit(field);
}
