{
  if (nullsFirst || nullsLast) {
switch (context.configuration().dialect().family()) {
case CUBRID:
case MARIADB:
case MYSQL:
case SQLITE:
{
        context.visit(field);
      }
  }
}
context.visit(field);
}
