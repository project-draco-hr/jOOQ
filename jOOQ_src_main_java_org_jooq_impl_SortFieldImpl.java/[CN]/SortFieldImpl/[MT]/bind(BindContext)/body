{
  if (nullsFirst || nullsLast) {
switch (context.configuration().dialect()) {
case DB2:
case ASE:
case CUBRID:
case INGRES:
case MYSQL:
case SQLITE:
case SQLSERVER:
case SYBASE:
{
        context.bind(field);
      }
  }
}
context.bind(field);
}
