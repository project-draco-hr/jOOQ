{
  if (nullsFirst || nullsLast) {
switch (context.configuration().getDialect()) {
case DB2:
case ASE:
case CUBRID:
case INGRES:
case MYSQL:
case SQLITE:
case SQLSERVER:
case SYBASE:
{
        context.bind(field);
      }
  }
}
context.bind(field);
}
