{
  if (context.subquery()) {
    context.keyword(operator.toSQL()).sql(" (").sql(query).sql(")");
  }
 else {
    context.keyword(operator.toSQL()).sql(" (").subquery(true).sql(query).subquery(false).sql(")");
  }
}
