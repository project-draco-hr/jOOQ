{
  if (context.subquery()) {
    context.sql(operator.toSQL()).sql(" (").sql(query).sql(")");
  }
 else {
    context.sql(operator.toSQL()).sql(" (").subquery(true).sql(query).subquery(false).sql(")");
  }
}
