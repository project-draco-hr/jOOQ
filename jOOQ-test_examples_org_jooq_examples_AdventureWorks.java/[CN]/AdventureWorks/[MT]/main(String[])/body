{
  Class.forName("com.microsoft.sqlserver.jdbc.SQLServerDriver");
  Connection connection=DriverManager.getConnection("jdbc:sqlserver://localhost:1433;databaseName=AdventureWorks;integratedSecurity=true");
  Executor create=new Executor(connection,SQLDialect.SQLSERVER);
  System.out.println(create.select(Employee.fields()).select(val("###")).select(Department.fields()).select(val("###")).select(Contact.fields()).from(Employee).join(EmployeeAddress).using(Employee.EmployeeID).join(EmployeeDepartmentHistory).using(Employee.EmployeeID).join(Department).using(Department.DepartmentID).join(Contact).using(Contact.ContactID).fetch());
}
