{
  if (context.subquery()) {
    context.sql("(").formatIndentLockStart().sql(query).formatIndentLockEnd().sql(")");
  }
 else {
    context.sql("(").subquery(true).formatIndentLockStart().sql(query).formatIndentLockEnd().subquery(false).sql(")");
  }
}
