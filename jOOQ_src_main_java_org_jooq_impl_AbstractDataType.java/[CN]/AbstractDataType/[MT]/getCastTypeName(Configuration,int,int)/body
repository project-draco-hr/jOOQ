{
  String result=getCastTypeName(configuration);
  if (precision != 0) {
    result=result.replaceAll("\\([^\\)]*\\)","");
    if (scale != 0) {
      result+="(" + precision + ", "+ scale+ ")";
    }
 else {
      result+="(" + precision + ")";
    }
  }
  return result;
}
