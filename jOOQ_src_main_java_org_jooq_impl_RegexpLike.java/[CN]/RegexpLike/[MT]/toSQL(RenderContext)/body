{
switch (context.configuration().dialect().family()) {
case CUBRID:
case H2:
case MARIADB:
case MYSQL:
case SQLITE:
case SYBASE:
{
      context.visit(search).keyword(" regexp ").visit(pattern);
      break;
    }
case HSQLDB:
{
    context.visit(DSL.condition("{regexp_matches}({0}, {1})",search,pattern));
    break;
  }
case POSTGRES:
{
  context.visit(DSL.condition("{0} ~ {1}",search,pattern));
  break;
}
case ORACLE:
{
context.visit(DSL.condition("{regexp_like}({0}, {1})",search,pattern));
break;
}
case ASE:
case DB2:
case DERBY:
case FIREBIRD:
case INGRES:
case SQLSERVER:
default :
{
context.visit(search).keyword(" like_regex ").visit(pattern);
break;
}
}
}
