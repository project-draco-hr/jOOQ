{
  sb.append("[");
  int size=paramList.size();
  for (int i=0; i < size; i++) {
    if (i > 0) {
      sb.append(", ");
    }
    String param=paramList.get(i);
    if (param != null) {
      sb.append(param);
    }
  }
  sb.append("]");
  isNew=true;
}
