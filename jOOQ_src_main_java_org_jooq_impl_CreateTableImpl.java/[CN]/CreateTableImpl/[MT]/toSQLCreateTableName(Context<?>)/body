{
  ctx.start(CREATE_TABLE_NAME).keyword("create").sql(' ');
  if (temporary)   if (asList(POSTGRES).contains(ctx.family()))   ctx.keyword("temporary").sql(' ');
 else   ctx.keyword("global temporary").sql(' ');
  ctx.keyword("table").sql(' ').visit(table).end(CREATE_TABLE_NAME);
}
