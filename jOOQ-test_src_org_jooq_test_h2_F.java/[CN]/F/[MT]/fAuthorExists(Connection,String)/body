{
  ContextDSL create=create(connection);
  Integer result=create.select(sign(count())).from(T_AUTHOR).where(TAuthor.FIRST_NAME.equal(authorName)).or(TAuthor.LAST_NAME.equal(authorName)).fetchOne(0,Integer.class);
  return result;
}
