{
  if (count == null || count.length == 0) {
    matchCount=null;
  }
 else {
    matchCount=new TreeSet<Integer>();
    for (    int i : count) {
      matchCount.add(i);
    }
  }
  apply();
  return this;
}
